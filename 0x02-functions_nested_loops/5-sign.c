#include <stdio.h>
/**
 * main -function that prints the sign of a number.
 *
 * Return:zero if no errorss, Non-zero value if errors.
 */
int main(void)
{
	float f  = 1.0;

	printf("%f%+f\n", f, f);

	return (0);
}

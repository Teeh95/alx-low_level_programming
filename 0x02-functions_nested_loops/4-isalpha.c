#include <stdio.h>
/**
 * main - function that checks for alphabetic character.
 *
 * Return:zero if no errors, Non-zero value if errors
 */
int main(void)
{
	char ch;

	printf("Pls check an alphabetic character\n")
	scanf("%c", &ch);

	return (0);
}

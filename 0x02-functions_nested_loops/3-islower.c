#include <stdio.h>
/**
 * main -checks for lowercase character.
 *
 * Return:zero if no error, Non-zero value if errors.
 */
int main(void)
{
	char ch;

	printf("Pls checks for lowercase character\n");
	scanf ("%c"; &ch);
	switch(ch)
	{
		case 'a' ... 'z':
			printf("%c is in a lowercase"; ch);
			break;

	}
	return (0)
}

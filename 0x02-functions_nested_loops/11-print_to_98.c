#include<stdio.h>
/**
 * main -prints all natural numbers from n to 98
 *
 * Return:zero
 */
int add(int, int);

int main(void)
{
	int i;

	for (i = 1; i <= 98; i++);

	printf("%d\n", i);

	return (0);
}

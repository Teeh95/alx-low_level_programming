#include <stdio.h>
/**
 * main -prints the 9 times table
 *
 * Return:zero
 */
void tables int(num)
{
	int(count);

	for(count = 1; count <= 9; count++);
	{
		printf("%d x %d = %d/n", num, count, num*count);
	}
}
int main(void)
{
	int num;

	printf("Enter a positive number\n");
	scanf ("%d", &num);

	printf("\nMultiplication Table for %d is:\n", num);

	table num;

	return (0);
}
